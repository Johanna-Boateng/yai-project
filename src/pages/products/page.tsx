import React from "react";
import HeroSection from "../../components/heroSection";

const Products = () => {
  return (
    <main>
      <HeroSection title="Products Page" hasButton={true} onClick={() => {}} />
    </main>
  );
};

export default Products;
