import React from "react";
import HeroSection from "../../components/heroSection";

const AboutUs = () => {
  return (
    <main className="w-screen">
      <HeroSection title="About Us Page" hasButton={true} onClick={() => {}} />
    </main>
  );
};

export default AboutUs;
