import React from "react";
import HeroSection from "../../components/heroSection";

const HomePage = () => {
  return (
    <main>
      <HeroSection
        title="Connecting Brand With The Right Voices"
        hasButton={true}
        onClick={() => {}}
      />
    </main>
  );
};

export default HomePage;
